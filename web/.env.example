# Vite dev server proxy target.
# Leave unset to proxy to a local backend at http://localhost:3000.
# Set to your deployed Render URL to skip running the backend locally.

# VITE_API_PROXY_TARGET=https://notes-app-2280.onrender.com
